import { colors } from '@utils/colors'

/**
 * Цвета материалов для Three.js/canvas
 * Читаются из CSS-переменных, определённых в src/index.css через @theme
 * Это обеспечивает единый источник цветовой схемы для CSS и JavaScript
 */
export const waterColor = colors.water
export const continentColor = colors.continent
export const itemColor = colors.item
export const aircraftColor = colors.aircraft
export const itemColorSelected = colors.itemSelected
export const itemColorHovered = colors.itemHovered
